version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATA_FILE=/data/domains.json
      - CLOUDFLARE_API_TOKEN
    volumes:
      - ./data:/data
